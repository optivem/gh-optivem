name: 'Run GoReleaser'
description: 'Sets up Go and runs GoReleaser with the optivem/shop SHA and tag baked into the build.'
inputs:
  shop-sha:
    description: 'optivem/shop commit SHA to bake into the build'
    required: true
  shop-tag:
    description: 'optivem/shop tag pointing at shop-sha (optional, for human-readable display)'
    required: false
    default: ''
  github-token:
    description: 'GitHub token for GoReleaser'
    required: true
runs:
  using: 'composite'
  steps:
    - name: Setup Go
      uses: actions/setup-go@v6
      with:
        go-version-file: go.mod
        cache: false

    - name: Setup Zig
      # Zig provides cross-compile toolchains for all six release targets
      # (linux/darwin/windows × amd64/arm64) from one Linux runner. The
      # per-target CC=zig cc -target … overrides in .goreleaser.yml depend
      # on this step having installed zig.
      uses: mlugg/setup-zig@v2
      with:
        version: 0.14.1

    - name: Run GoReleaser
      uses: goreleaser/goreleaser-action@v7
      with:
        version: latest
        args: release --clean
      env:
        GITHUB_TOKEN: ${{ inputs.github-token }}
        SHOP_SHA: ${{ inputs.shop-sha }}
        SHOP_TAG: ${{ inputs.shop-tag }}
