# Plan: gh optivem atdd install/upgrade + init wiring

> **Depends on**: `shop/.plans/20260427-091546-atdd-claude-distribution.md` — must complete first (shop must host the canonical ATDD assets before the CLI can copy from them).

## Problem

Shop hosts canonical ATDD Claude assets (agents, commands, prompts) under `.claude/agents/atdd/`, `.claude/commands/atdd/`, `docs/prompts/`. We need a CLI to distribute them to:

1. Existing repos via `gh optivem atdd install`.
2. Student repos newly scaffolded via `gh optivem init`.

## Source of truth: shop, not gh-optivem

`gh optivem init` already treats **shop** as the canonical source for everything it distributes — workflows, system code, externals, system-tests, docs. The CLI does not embed templates: at release time `goreleaser` bakes a specific shop SHA into the binary via ldflags (`internal/version.ShopRef`), and at runtime `--shop-ref <tag|sha|branch>` pins which shop checkout to copy from. `ApplyTemplate` walks `cfg.ShopPath` and copies files, applying string-replacement maps (`monolithContentReplacements`, `FixupWorkflowContent`, etc.) at copy time.

**ATDD assets follow the same pattern**: live in `shop/.claude/` and `shop/docs/prompts/`, copied by `gh optivem atdd install` from `cfg.ShopPath`. No `embed.FS`, no per-asset versioning, no template extraction step.

This rules out the earlier "extract into `gh-optivem/internal/templates/atdd/`" approach: it would duplicate the source-of-truth that `init` already establishes and require its own release/versioning UX.

## Architecture-agnosticism

ATDD doctrine is architecture-agnostic. The cycle (RED → GREEN at three layers: TEST, DSL, DRIVER) and the contract-test sub-process don't depend on monolith vs. multitier, monorepo vs. multirepo, or backend language. A single set of templates serves every `gh optivem init` arch+strategy combination. No per-arch ATDD variants.

## Repo layout assumptions

- **shop**: monorepo. System and tests live in the same repo.
- **`gh optivem init` scaffolds**:
  - Monorepo: one repo, everything in it.
  - Multirepo monolith: `<repo>` + `<repo>-system`.
  - Multirepo multitier: `<repo>` + `<repo>-backend` + `<repo>-frontend`.
  - In all cases, the **root repo** (`<repo>`) is the orchestration/docs/tests home.
- **eshop-tests** (legacy, archived): a special case the v1 installer does **not** need to support.

**Decision**: install `.claude/` and `docs/prompts/` into the root repo only, regardless of mono vs. multirepo. The agents themselves can still operate across sibling repos (via `--system-repos` flags they already accept), but their definitions live in one place. No `--test-repo` flag in v1.

## Templating

Follow the existing `ApplyTemplate` pattern: shop's files contain literal strings (e.g., `shop` for the SUT name); `gh optivem atdd install` applies a replacement map at copy time:

- `shop` → `cfg.SystemRepo` (the student's repo name, defaults to current `git remote` repo name).

`Run-SystemTests.ps1` was already replaced in shop (Phase 1 of the source plan), so no further substitution is needed at install time.

A post-condition validator (mirroring `ValidateNoLeftoverTemplateRefs` in `apply_template.go`) fails if any forbidden literal survives. Note: the generic ATDD-doctrine `shop/` package convention is distinct from the SUT name and must NOT be in the replacement map — the two uses must be textually distinguishable (e.g., `shop` as SUT name appears bare or in URLs; `shop/` as package appears with a trailing slash in code paths).

## Install behavior

**Full replace**, idempotent. `gh optivem atdd install` deletes every managed file and writes fresh content from shop at the binary's baked-in `cfg.ShopRef`. No merge logic, no version banners, no "are you sure" prompts for files that haven't been edited (because they're not supposed to be edited). Re-running is safe: same binary + same shop content = byte-identical result.

**No separate `atdd upgrade` command.** ATDD ships *as part of shop* and is versioned with the gh-optivem release that pinned that shop SHA. The canonical upgrade flow is:

1. `gh optivem upgrade` — installs a newer `gh optivem` binary with a newer baked-in shop ref. (Thin wrapper around `gh extension upgrade optivem`.)
2. `gh optivem atdd install` — re-installs against the new ref.

Even slicker: `gh optivem atdd install --auto-upgrade` self-upgrades and re-execs in one shot (using the existing `checkForUpdate`/`--auto-upgrade` plumbing in `main.go`).

Manual `--shop-ref <ref>` exists as an internal/dev override (e.g., to test newer shop content before a release) but is not the recommended user-facing upgrade UX.

**Pre-flight check**: warn (and abort without `--force`) if any managed file's content hash differs from what shop has at `cfg.ShopRef` — to catch the case of a student editing in place by mistake. With `--force`, replace anyway.

**Ownership rule** — managed file set:

- `.claude/agents/atdd-*.md` (flat layout, `atdd-` filename prefix)
- `.claude/commands/atdd-*.md` (flat layout, `atdd-` filename prefix)
- Everything under `docs/prompts/{atdd,architecture,code}/` (subdir layout — these are doctrine docs, not Claude Code agent/command files)

Students must NOT edit these in place. To customize: copy the file, rename to drop the `atdd-` prefix (e.g. `atdd-backend.md` → `my-backend.md`), and edit the copy. The original stays untouched and upgrades cleanly.

**Why this layout**:

- `.claude/agents/` and `.claude/commands/` use a flat `atdd-` prefix because Claude Code documentation does not confirm subdir support for agents/commands (verified 2026-04-27); subdirs may not be discovered.
- `docs/prompts/` uses subdirs because they're plain doctrine docs Claude reads through `@docs/prompts/...` references in agent files — directory layout is up to the author.
- The `atdd-` prefix is an unambiguous ownership signal (every matching file belongs to the tool).
- Doesn't collide with student-authored agents/commands at the root (any name not matching `atdd-*` is preserved).
- Trivial wholesale upgrade (`rm .claude/agents/atdd-*.md` then copy fresh).
- Removes versioning/upgrade-merge complexity entirely.

## Tasks

### Phase 2 — `gh optivem atdd install` command

> **Multirepo support note**: shop is a monorepo, so its canonical ATDD content collapses test/system references to a single `shop` repo. When installing into a multirepo scaffold (`<repo>` + `<repo>-backend` + `<repo>-frontend`, or `<repo>` + `<repo>-system`), the install-time substitution needs to expand `shop` into multiple repo names rather than a single `cfg.SystemRepo`. Search the imported assets for `TODO(gh-optivem): multirepo support` — those are the spots that need handling. v1 may ship monorepo-only and surface multirepo expansion as a follow-up.

- [ ] Add subcommand `gh optivem atdd install` (in `main.go` / `runner_commands.go`).
- [ ] Resolve `cfg.ShopPath` and `cfg.ShopRef` via the same `resolveShopRef` mechanism `init` uses.
- [ ] Flags:
  - `--system-repo <name>` (defaults to current repo name detected via `git remote`).
  - `--shop-ref <ref>` — **internal/dev override only**, not the recommended upgrade UX. Defaults to the binary's baked-in shop SHA (set by `goreleaser` ldflags). Document that the canonical upgrade flow is `gh optivem upgrade` followed by `gh optivem atdd install` (or `gh optivem atdd install --auto-upgrade` to do both in one shot).
  - `--force` to overwrite existing files / pre-flight failures.
  - `--dry-run`.
- [ ] Copy `shop/.claude/agents/atdd-*.md` → `./.claude/agents/atdd-*.md` (full replace; flat layout with `atdd-` filename prefix — Claude Code does not confirm subdir support for agents).
- [ ] Copy `shop/.claude/commands/atdd-*.md` → `./.claude/commands/atdd-*.md` (full replace; same flat-prefix reason).
- [ ] Copy `shop/docs/prompts/{atdd,architecture,code}/` → `./docs/prompts/{atdd,architecture,code}/` (full replace; `glossary.md` lives under `atdd/`).
- [ ] Apply replacement map (`shop` → `cfg.SystemRepo`) using the existing `templates.FixupAllTextFiles` helper.
- [ ] **Multirepo expansion** (decide v1 scope): for multitier consumers, replace single-repo references with `cfg.SystemRepo-backend`/`-frontend`; for multirepo monolith, with `cfg.SystemRepo-system`. See `TODO(gh-optivem): multirepo support` markers in the imported ATDD content (manager.md, acceptance-tests.md, manage-project.md, implement-ticket.md). If deferred, fail loudly when invoked in a multirepo scaffold so users aren't silently misled.
- [ ] Pre-flight: hash-check installed copies against shop at `cfg.ShopRef`; abort without `--force` if any managed file diverges.
- [ ] Post-condition: validator fails if forbidden literals survive in the installed assets — mirror `ValidateNoLeftoverTemplateRefs`.
- [ ] No `--test-repo` flag in v1 — tests are always in the same repo as the agents.

### Phase 2.5 — optional CLI ergonomics

- [ ] Consider adding `--rebuild` flag to `gh optivem test system` so the imported ATDD prompts can collapse the two-command pattern (`gh optivem build system --rebuild` then `gh optivem test system --suite x --test y`) back to a single line. Source plan substituted these as two-line forms because `test system` has no `--rebuild` flag today (only `build system` does). Search the imported ATDD content for `gh optivem build system --rebuild` to find every spot affected.

### Phase 4 — wire into `gh optivem init`

- [ ] After `ApplyTemplate` succeeds, call `atdd install` with the same `cfg.ShopPath` / `cfg.ShopRef` / `cfg.SystemRepo`.
- [ ] Add `--no-atdd` to skip; default is on.
- [ ] Remove the `CLAUDE.md` skip in `gh-optivem/internal/files/files.go:205-207` and the `.claude` skip at line 195 — the new install path supersedes them.

### Phase 5 — retrofit existing repos

- [ ] Run `gh optivem atdd install --system-repo shop` inside shop itself to verify idempotence (for shop, `cfg.SystemRepo == shop`, so the substitution is a no-op and the install should be a content-identical copy).
- [ ] Leave eshop-tests as-is (archived per memory) — it stays as the historical source the templates were extracted from.

## Open questions

1. **CLAUDE.md generation.** The existing TODO at `files.go:203` wants per-project CLAUDE.md. Out of scope unless we want to bundle it — recommend deferring.
2. **Release coupling.** Once ATDD assets ship as part of shop, a doctrine fix is gated on a shop `meta-v*` release. Acceptable cost; flagging here for visibility.

## Non-goals

- Changing the ATDD doctrine itself.
- Updating eshop-tests (archived per memory).
- Building a per-language ATDD variant.
- Embedding ATDD templates in the `gh-optivem` binary (rejected — see Option B below).

## Options considered (historical)

### Option A — copy `.claude/` and `docs/prompts/` directly into shop, no installer

- Pro: zero infra, students who clone shop get it for free.
- Con: doesn't help any repo other than shop; hardcoded SUT names have to be hand-edited per consumer; no story for `gh optivem init` scaffolds.

### Option B — extract into `gh-optivem/internal/templates/atdd/`, distribute via embed.FS

- Pro: self-contained binary, doctrine versioned with the CLI.
- Con: duplicates the source-of-truth that `init` already establishes (shop); requires its own release/versioning UX; templates rot away from the live SUT they're meant to exercise.
- Rejected.

### Option C — shop hosts canonical assets, `gh optivem atdd install` copies from `cfg.ShopPath` (CHOSEN)

- Single source of truth lives in `shop/.claude/agents/atdd/`, `shop/.claude/commands/atdd/`, `shop/docs/prompts/`.
- `gh optivem atdd install` (run inside any repo) copies from `cfg.ShopPath` into the current repo, applying the templating replacement map.
- `gh optivem init` calls the same installer at the end of scaffolding (gated by `--no-atdd`, default on).
- Versioning is free: `--shop-ref` (and the baked-in SHA in released binaries) already pins which shop revision is the canonical source.
- Existing TODO at `gh-optivem/internal/files/files.go:203` already anticipates this ("When ATDD support is added, generate project-specific CLAUDE.md files…").
- Pro: domain-agnostic, single place to author, students self-serve, retrofits any existing repo, dogfooded against shop's live code, no new versioning surface.
- Con: ATDD assets become part of shop's release surface — a doctrine fix is gated on a shop `meta-v*` release.
