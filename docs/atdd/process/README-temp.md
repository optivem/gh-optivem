# ATDD process docs — WORK IN PROGRESS

> 🚧 The docs in this folder ([`change/behavior/at-cycle.md`](change/behavior/at-cycle.md), [`change/behavior/ct/ct-cycle.md`](change/behavior/ct/ct-cycle.md)) are being built up to become the canonical ATDD process spec, to be referenced by the per-phase agent prompts under `internal/assets/runtime/prompts/atdd/`. The goal is to make those prompts significantly leaner — each prompt should reference the relevant section of these docs rather than restating the cycle rules inline. Once these docs reach parity with the global process pages (`internal/assets/global/docs/atdd/process/{at,ct}-*.md`), those eight global pages will be deleted and the runtime prompts will be slimmed to a thin pointer + per-phase operational notes.

This README is `README-temp.md` rather than `README.md` because it documents the WIP state, not the folder's permanent purpose. It is expected to be removed (or rewritten) once the migration above is complete.
