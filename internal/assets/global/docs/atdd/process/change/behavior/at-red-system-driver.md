# AT - RED - SYSTEM DRIVER

Implement the System Driver adapters (only if `System Driver Interface Changed = yes`).

## Scope

This phase touches the `driver_port`, `driver_adapter` layers (bare
layer names; resolved physical paths live in `gh-optivem.yaml paths:`
— inspect with `gh optivem process scope AT_RED_SYSTEM_DRIVER`).

See [the scope rule](../../shared/scope.md).

## Steps

1. Implement the System Driver Adapters for real - replace each "TODO: System Driver" prototype with actual logic.
